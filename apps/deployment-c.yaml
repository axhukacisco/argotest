apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-c
  template:
    metadata:
      labels:
        app: service-c
    spec:
      containers:
      - name: service-c
        image: python:3.10-slim
        command: ["sh", "-c"]
        args:
          - pip install flask requests && python /app/service-c.py
        ports:
        - containerPort: 5000
        volumeMounts:
        - name: script-volume
          mountPath: /app/service-c.py
          subPath: service-c.py
      volumes:
      - name: script-volume
        configMap:
          name: service-c-script
